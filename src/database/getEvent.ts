import '../lib/supabase';
import { getSupabaseClient } from '../lib/supabase';

export const getEvent: any = async(eventId: number) => {
  /*const { data, error } = await getSupabaseClient()
    .from('event')
    .select()
    .eq('id', eventId);

  return data*/
}